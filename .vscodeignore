.vscode/**
.vscode-ext-debug/**
.gitignore
.env
.claude/**
.github/**
.grok/**
*.log
# Local tool state. These are not source and some hold credentials — a
# packaged .vsix was shipping .gstack/terminal-internal-token before this.
.gstack/**
.prgraf/**
.serena/**
.swarm/**
.beads/**
**/*.vsix
docs/**
docs/research/**
scripts/**
test/**
src/**
tsconfig.json
package-lock.json
pnpm-lock.yaml
node_modules/**
# (no runtime dependencies: package.json 'dependencies' is empty, and
#  packaging runs with --no-dependencies, so node_modules is never walked)
.git/**
**/*.map
**/*.ts
!out/**/*.js
